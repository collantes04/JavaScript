let contadorAlerta = 1;

for(let i = 0; i<2; i ++){
    //El for itera dos veces duplicando la alerta
    /*Mientras que el contador dice que numero de alerta es*/
    window.alert("Alerta " + contadorAlerta);
    contadorAlerta++;
}